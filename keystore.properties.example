# Copy this file to keystore.properties (repo root) and fill in real values.
# keystore.properties is gitignored -- never commit it or the .jks file itself.
#
# storeFile is resolved relative to the repo root.

storeFile=pocket-bible-release.jks
storePassword=REPLACE_ME
keyAlias=pocketbible
keyPassword=REPLACE_ME
